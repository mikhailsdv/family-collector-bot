{
  "name": "collector-bot",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file=.env dist/index.js",
    "lint": "eslint .",
    "format": "prettier . --write",
    "check-format": "prettier . --check",
    "docker": "docker compose down --remove-orphans -v && docker compose up --build -d"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "grammy": "1.41.1"
  },
  "devDependencies": {
    "@types/node": "25.3.5",
    "eslint": "10.0.3",
    "prettier": "3.8.1",
    "typescript": "5.9.3"
  }
}